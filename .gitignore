# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node, only if someone adds a static server
node_modules/

# Exported PDFs of the deck
*.pdf

# Agent skills, vendored by `npx skills add`. Not committed: .claude/skills
# holds an absolute symlink that only resolves on the machine that installed it.
# skills-lock.json IS committed — it pins the source and hash, so anyone can
# reproduce the install with `npx skills add nutlope/hallmark`.
.agents/
.claude/
